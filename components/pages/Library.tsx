
import React from 'react';
import PagePlaceholder from './PagePlaceholder';

const Library: React.FC = () => {
    return (
        <PagePlaceholder
            title="Library"
            icon="fa-book-bookmark"
            description="All your saved notes, solved questions, and study plans are kept here for easy access anytime."
        />
    );
};

export default Library;
